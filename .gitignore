# Contains personal info (phone number) and isn't meant to sync to the
# public repo via .github/workflows/sync-to-public.yml.
claude_context/portfolio_context.md

# Windows "downloaded from the internet" marker files, not meant to be tracked.
*:Zone.Identifier
